# Customization

Rename the package with `setup_repo.sh`, then replace the sample greeting API
with project code.

Keep package names consistent:

- Cargo package and binary names use kebab-case.
- Rust library crate names use snake_case.
- Repository URLs should match the new GitHub owner and repository.
