# Documentation

Write API documentation in Rust doc comments and user-facing guides under
`docs/`.

Build Rust API docs:

```shell
cargo doc --no-deps --all-features
```

Build the documentation site when Zensical is installed:

```shell
zensical build
```
