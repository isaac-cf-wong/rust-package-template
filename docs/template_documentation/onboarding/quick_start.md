# Template Quick Start

1. Create a repository from this template.
2. Clone the new repository.
3. Run `./setup_repo.sh` after setting `origin` to the new GitHub repository.
4. Update `Cargo.toml` metadata, README badges, and documentation.
5. Run `cargo test` before committing.
